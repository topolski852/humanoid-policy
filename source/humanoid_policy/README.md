# humanoid_policy

Isaac Lab task/environment definitions for training the humanoid robot's control policies.

Contains the locomotion task variants (`walk`, `standup`) registered as gym environments and
consumed by the training scripts in `scripts/rsl_rl/`.
